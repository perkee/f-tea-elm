{
  "scripts": {
    "dev": "parcel index.html --open",
    "build-local": "rm -r dist; parcel build index.html --detailed-report --no-source-maps --public-url='/powerlifting' && ls -l dist && echo \"compiled elm-powerlifting @ $(git rev-parse --short HEAD)\" && echo \"compiled elm-powerlifting @ $(git rev-parse --short HEAD)\" | pbcopy",
    "build": "rm -r dist; parcel build index.html --detailed-report --no-source-maps --public-url='/'",
    "analyze": "elm-analyse --serve --port=1235 --open"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "elm": "^0.19.0-no-deps",
    "font-awesome": "^4.7.0",
    "lodash": "^4.17.13",
    "parcel-bundler": "^1.12.3",
    "set-value": "^2.0.1"
  },
  "devDependencies": {
    "elm-format": "^0.8.2",
    "elm-hot": "^1.0.1",
    "eslint": "^6.6.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "node-elm-compiler": "^5.0.3"
  }
}
